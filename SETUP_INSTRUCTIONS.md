# 🚀 Инструкция по настройке проекта

## ✅ ЧТО БЫЛО ИСПРАВЛЕНО

### 1. 🔒 **Безопасность и авторизация**
- ✅ Добавлена проверка авторизации через Telegram на frontend
- ✅ Пользователь **НЕ СМОЖЕТ** пользоваться WebApp без авторизации
- ✅ При отсутствии авторизации показывается блокирующий экран
- ✅ Приложение автоматически закрывается через 5 секунд

### 2. 🌐 **CORS и API**
- ✅ Исправлены CORS настройки (убраны wildcards)
- ✅ Создан конфиг для API URL (`frontend/src/shared/config/api.js`)
- ✅ API endpoints вынесены в константы

### 3. ✔️ **Валидация данных**
- ✅ Добавлена валидация телефона в API
- ✅ Валидация текстовых полей (trim, length)
- ✅ Понятные сообщения об ошибках

### 4. 📋 **Логирование**
- ✅ Настроена ротация логов (max 10MB, 5 backup файлов)
- ✅ Уменьшен уровень логирования httpx и telegram.ext
- ✅ Раздельные логи для бота и API

### 5. 🛡️ **Обработка ошибок**
- ✅ При ошибке БД приложение останавливается (не продолжает работу)
- ✅ Улучшены сообщения об ошибках

### 6. 🗂️ **Файловая структура**
- ✅ Создан `.gitignore` для защиты секретов
- ✅ Создан `env.example` как шаблон

---

## ⚡ НАСТРОЙКА ЗА 5 МИНУТ

### Шаг 1: Создайте файл `.env`

```bash
cd backend
cp env.example .env
```

### Шаг 2: Получите новый токен бота

**🚨 ВАЖНО:** Ваш старый токен был скомпрометирован в логах!

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите "API Token" → "Revoke current token"
5. Скопируйте новый токен

### Шаг 3: Отредактируйте `.env`

```bash
nano .env  # или используйте любой редактор
```

Вставьте ваш токен:

```env
BOT_TOKEN=ваш_новый_токен_здесь
WEB_APP_URL=https://zhoasss.github.io/frontend/index.html
```

### Шаг 4: Удалите старые логи из git

```bash
cd backend
git rm --cached bot.log api.log database.db
git commit -m "Remove sensitive files from git"
```

### Шаг 5: Запустите проект

**Терминал 1 - API сервер:**
```bash
cd backend
python api_server.py
```

**Терминал 2 - Telegram бот:**
```bash
cd backend
python main.py
```

### Шаг 6: Протестируйте

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите "Открыть кабинет 🚀"
4. **Проверка авторизации:**
   - Если открыли через бота → всё работает ✅
   - Если открыли напрямую → доступ заблокирован 🔒

---

## 🔒 КАК РАБОТАЕТ ЗАЩИТА АВТОРИЗАЦИИ

### Frontend (`frontend/src/app/providers/auth/guard.js`)

**При загрузке КАЖДОЙ страницы проверяется:**

1. ✅ Наличие Telegram WebApp
2. ✅ Валидность initData
3. ✅ Наличие данных пользователя (user.id)
4. ✅ Корректность hash в initData

**Если проверка не пройдена:**
- ❌ Показывается блокирующий экран с ошибкой
- ❌ Основной контент скрывается
- ❌ Через 5 секунд приложение закрывается

### Backend (`backend/src/shared/auth/telegram_auth.py`)

**Каждый API запрос проверяет:**

1. ✅ Наличие заголовка `X-Init-Data`
2. ✅ HMAC-SHA256 подпись от Telegram
3. ✅ Соответствие hash и данных
4. ✅ Валидность токена бота

**Если проверка не пройдена:**
- ❌ HTTP 401 Unauthorized
- ❌ Детальное сообщение об ошибке

---

## 📁 НОВЫЕ ФАЙЛЫ

### Frontend:
```
frontend/src/
├── shared/
│   ├── config/
│   │   └── api.js                    # ✨ Конфигурация API
│   └── lib/
│       └── auth.js                   # ✨ Модуль авторизации
└── app/
    └── providers/
        └── auth/
            └── guard.js              # ✨ Auth Guard (защита)
```

### Backend:
```
backend/
├── .gitignore                        # ✨ Игнорируемые файлы
└── env.example                       # ✨ Шаблон .env
```

---

## 🧪 ТЕСТИРОВАНИЕ

### 1. Проверка авторизации

**Тест 1: Открытие через бота (должно работать)**
```
1. Откройте бота в Telegram
2. /start
3. Нажмите "Открыть кабинет"
4. ✅ Приложение открывается
5. ✅ Данные профиля загружаются
```

**Тест 2: Открытие напрямую (должно блокироваться)**
```
1. Откройте в браузере: https://zhoasss.github.io/frontend/index.html
2. ❌ Показывается экран "Доступ запрещен"
3. ❌ Через 5 секунд закроется (если в Telegram)
```

### 2. Проверка API

```bash
# Проверка здоровья
curl http://localhost:8000/health

# Без авторизации - должен вернуть 401
curl http://localhost:8000/api/profile/
```

### 3. Проверка логов

```bash
# Размер лога не должен превышать 10MB
ls -lh backend/*.log

# Должны быть отдельные файлы
ls backend/bot.log backend/api.log
```

---

## 📊 ЧТО ИЗМЕНИЛОСЬ В КОДЕ

### 1. `api_server.py`
```python
# ДО:
allow_origins=["http://localhost:*"]  # ❌ Не работает

# ПОСЛЕ:
allow_origins=[
    "https://zhoasss.github.io",
    "http://localhost:5173",
    "http://localhost:3000",
]  # ✅ Конкретные порты
```

### 2. `profile-api.js`
```javascript
// ДО:
const API_BASE_URL = 'http://localhost:8000';  // ❌ Захардкожено

// ПОСЛЕ:
import { API_BASE_URL } from '../config/api.js';  // ✅ Из конфига
```

### 3. `main.py`
```python
# ДО:
except Exception as e:
    logging.error(...)
    logging.warning("⚠️ Бот продолжит работу без БД")  # ❌ Опасно!

# ПОСЛЕ:
except Exception as e:
    logging.critical(...)
    raise SystemExit(1)  # ✅ Останавливаем при ошибке
```

### 4. `setup.py` (logger)
```python
# ДО:
logging.basicConfig(...)  # ❌ Нет ротации

# ПОСЛЕ:
file_handler = RotatingFileHandler(
    log_path,
    maxBytes=10*1024*1024,  # 10MB
    backupCount=5
)  # ✅ С ротацией
```

### 5. HTML страницы
```html
<!-- ДО: -->
<script src="telegram/init.js"></script>
<script src="config/theme.js"></script>

<!-- ПОСЛЕ: -->
<script src="telegram/init.js"></script>
<script src="auth/guard.js"></script>  <!-- ✨ Auth Guard -->
<script src="config/theme.js"></script>
```

---

## 🎯 ПРОВЕРОЧНЫЙ СПИСОК

Перед использованием убедитесь:

- [ ] Создан файл `backend/.env` с новым токеном
- [ ] Старые логи удалены из git
- [ ] `.gitignore` содержит `.env` и `*.log`
- [ ] API сервер запущен на порту 8000
- [ ] Telegram бот запущен и отвечает
- [ ] Открытие через бота работает
- [ ] Прямое открытие блокируется
- [ ] Данные профиля загружаются и сохраняются

---

## 🚨 ВАЖНЫЕ НАПОМИНАНИЯ

### ⚠️ НЕ ЗАБУДЬТЕ:
1. **Отозвать старый токен** через @BotFather
2. **Не коммитить** файл `.env` в git
3. **Удалить старые логи** из git репозитория

### 🔐 БЕЗОПАСНОСТЬ:
- ✅ Токен бота защищен (.env в .gitignore)
- ✅ Логи не попадают в git
- ✅ База данных не в git
- ✅ Авторизация обязательна для всех страниц
- ✅ Backend проверяет каждый запрос

### 📱 ДЛЯ PRODUCTION:
Когда будете деплоить на сервер:
1. Замените SQLite на PostgreSQL
2. Настройте HTTPS для API
3. Укажите production URL в `api.js`
4. Настройте reverse proxy (nginx)
5. Используйте systemd для автозапуска

---

## 🎉 ГОТОВО!

Теперь ваш проект:
- 🔒 **Защищен** от неавторизованного доступа
- ✅ **Валидирует** все входные данные
- 📋 **Логирует** с ротацией
- 🛡️ **Обрабатывает** ошибки корректно
- 🌐 **Работает** через CORS правильно

**Вопросы?** Проверьте файлы:
- `SECURITY_REVIEW.md` - детальный анализ
- `QUICK_FIXES.md` - готовые решения
- `PROJECT_SUMMARY.md` - краткая сводка

**Успехов! 🚀**

