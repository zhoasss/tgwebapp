# 🚀 Деплой: Публичное бронирование + Уведомления

## Что было добавлено

### Backend:
✅ Система уведомлений (`backend/src/shared/notifications/`)
✅ Интеграция уведомлений в публичное бронирование
✅ TelegramNotifier класс для отправки сообщений

### Frontend:
✅ Полностью готовая страница бронирования
✅ CSS стили с премиум дизайном
✅ JavaScript логика с валидацией
✅ Интеграция в настройки

### Документация:
✅ NOTIFICATIONS_SYSTEM.md
✅ PUBLIC_BOOKING_COMPLETE_V2.md

---

## 📋 Чеклист перед деплоем

- [ ] Все изменения закоммичены
- [ ] Проверена работа локально
- [ ] Обновлены зависимости (если нужно)
- [ ] Проверены миграции БД

---

## 🔧 Шаг 1: Локальное тестирование

### 1.1 Проверка backend

```bash
cd /Users/romannavickij/Project/zhoasss.github.io

# Проверить, что новые файлы на месте
ls -la backend/src/shared/notifications/

# Должны быть:
# __init__.py
# telegram_notifier.py
```

### 1.2 Проверка frontend

```bash
# Проверить файлы бронирования
ls -la frontend/src/pages/booking/

# Должны быть:
# index.html
# booking.css
# booking.js
```

### 1.3 Запуск локально (опционально)

```bash
# Запустить Docker
make dev

# Или
docker-compose up --build

# Проверить логи
docker-compose logs -f backend
```

---

## 📦 Шаг 2: Коммит изменений

```bash
cd /Users/romannavickij/Project/zhoasss.github.io

# Проверить статус
git status

# Добавить новые файлы
git add backend/src/shared/notifications/
git add backend/src/features/api/public_booking.py
git add NOTIFICATIONS_SYSTEM.md
git add PUBLIC_BOOKING_COMPLETE_V2.md
git add DEPLOY_BOOKING_NOTIFICATIONS.md

# Создать коммит
git commit -m "feat: Добавлена система уведомлений и завершено публичное бронирование

✨ Новые функции:
- Telegram уведомления о новых записях
- Уведомления об изменении статуса
- Напоминания о записях (API готов)
- Полностью рабочая страница публичного бронирования
- Премиум дизайн с анимациями
- Генерация временных слотов
- Валидация и маски ввода

🔧 Технические улучшения:
- Модуль TelegramNotifier
- Интеграция с публичным API
- Обработка ошибок уведомлений
- Подробная документация

📚 Документация:
- NOTIFICATIONS_SYSTEM.md
- PUBLIC_BOOKING_COMPLETE_V2.md
- DEPLOY_BOOKING_NOTIFICATIONS.md"

# Запушить на GitHub
git push origin main
```

---

## 🌐 Шаг 3: Деплой на сервер

### 3.1 Подключение к серверу

```bash
# У вас уже открыто SSH соединение
# Если нет, подключитесь:
ssh root@178.72.136.133
```

### 3.2 Обновление кода

```bash
# Перейти в директорию проекта
cd /root/zhoasss.github.io

# Создать backup БД (ВАЖНО!)
cp backend/database.db backend/database.db.backup.$(date +%Y%m%d_%H%M%S)

# Получить последние изменения
git pull origin main

# Проверить, что файлы обновились
ls -la backend/src/shared/notifications/
```

### 3.3 Перезапуск сервисов

```bash
# Остановить контейнеры
docker-compose down

# Пересобрать и запустить
docker-compose up --build -d

# Проверить статус
docker-compose ps

# Все сервисы должны быть в статусе "Up"
```

### 3.4 Проверка логов

```bash
# Проверить логи backend
docker-compose logs -f backend

# Должны увидеть:
# ✅ База данных инициализирована
# 🎯 API сервер готов к работе

# Ctrl+C для выхода
```

---

## ✅ Шаг 4: Проверка работоспособности

### 4.1 Проверка API

```bash
# На сервере
curl http://localhost/health

# Должен вернуть:
# {"status":"ok","service":"api",...}
```

### 4.2 Проверка через Telegram

1. **Откройте Telegram бот**
2. **Запустите Mini App**
3. **Перейдите в Настройки**
4. **Проверьте карточку "Ссылка для записи"**
   - Если ссылки нет, нажмите "✨ Создать ссылку"
   - Скопируйте ссылку

### 4.3 Тестирование публичного бронирования

1. **Откройте ссылку в браузере** (не в Telegram)
   ```
   https://booking-cab.ru/booking/ваш_slug
   ```

2. **Проверьте, что отображается:**
   - ✅ Профиль мастера
   - ✅ Список услуг
   - ✅ Выбор даты
   - ✅ Временные слоты
   - ✅ Форма записи

3. **Создайте тестовую запись:**
   - Выберите услугу
   - Выберите дату и время
   - Заполните контакты
   - Нажмите "Записаться"

4. **Проверьте Telegram:**
   - Должно прийти уведомление о новой записи
   - С полной информацией о клиенте и услуге

5. **Проверьте в Mini App:**
   - Откройте "Записи"
   - Должна появиться новая запись со статусом "Ожидает"

---

## 🐛 Troubleshooting

### Проблема: Уведомления не приходят

**Решение:**
```bash
# Проверить логи
docker-compose logs backend | grep -i "notification"

# Проверить BOT_TOKEN
docker-compose exec backend env | grep BOT_TOKEN

# Если токена нет, добавить в .env:
echo "BOT_TOKEN=ваш_токен" >> .env

# Перезапустить
docker-compose restart backend
```

### Проблема: Страница бронирования не загружается

**Решение:**
```bash
# Проверить nginx
docker-compose logs frontend

# Проверить доступность файлов
docker-compose exec frontend ls -la /usr/share/nginx/html/src/pages/booking/

# Перезапустить frontend
docker-compose restart frontend
```

### Проблема: Ошибка при создании записи

**Решение:**
```bash
# Проверить логи backend
docker-compose logs backend --tail=50

# Проверить миграции
docker-compose exec backend alembic current

# Если миграции не применены:
docker-compose exec backend alembic upgrade head
```

### Проблема: Временные слоты не отображаются

**Решение:**
1. Проверьте, что настроен график работы
2. Откройте Mini App → Настройки → График работы
3. Установите рабочие часы для нужных дней
4. Сохраните изменения

---

## 📊 Мониторинг

### Проверка здоровья сервисов

```bash
# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# Логи в реальном времени
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

### Проверка базы данных

```bash
# Подключиться к БД
docker-compose exec backend sqlite3 /app/data/database.db

# Проверить записи
SELECT COUNT(*) FROM appointments;

# Проверить уведомления (в логах)
.quit
docker-compose logs backend | grep "Notification sent"
```

---

## 🔄 Откат (если что-то пошло не так)

### Откат кода

```bash
# На сервере
cd /root/zhoasss.github.io

# Откатить к предыдущему коммиту
git reset --hard HEAD~1

# Перезапустить
docker-compose down
docker-compose up -d
```

### Восстановление БД

```bash
# Найти backup
ls -la backend/database.db.backup.*

# Восстановить
cp backend/database.db.backup.YYYYMMDD_HHMMSS backend/database.db

# Перезапустить
docker-compose restart backend
```

---

## 📈 Следующие шаги

После успешного деплоя:

1. **Протестируйте с реальными клиентами**
   - Отправьте ссылку нескольким клиентам
   - Соберите обратную связь

2. **Настройте напоминания** (опционально)
   - Реализуйте планировщик для автоматических напоминаний
   - См. NOTIFICATIONS_SYSTEM.md

3. **Добавьте аналитику**
   - Отслеживайте количество записей через публичную ссылку
   - Анализируйте популярные услуги

4. **Оптимизируйте**
   - Добавьте кэширование для профиля и услуг
   - Настройте CDN для статики

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи:** `docker-compose logs -f backend`
2. **Проверьте документацию:**
   - NOTIFICATIONS_SYSTEM.md
   - PUBLIC_BOOKING_COMPLETE_V2.md
   - QUICK_START.md
3. **Проверьте переменные окружения:** `.env` файл

---

## ✅ Контрольный список

- [ ] Код закоммичен и запушен
- [ ] Подключились к серверу
- [ ] Создали backup БД
- [ ] Выполнили `git pull`
- [ ] Перезапустили контейнеры
- [ ] Проверили логи (нет ошибок)
- [ ] Проверили health endpoint
- [ ] Создали тестовую ссылку
- [ ] Протестировали публичное бронирование
- [ ] Получили уведомление в Telegram
- [ ] Запись появилась в Mini App

---

**Дата создания:** 2025-12-01  
**Версия:** 1.0.0  
**Статус:** ✅ Ready to Deploy
