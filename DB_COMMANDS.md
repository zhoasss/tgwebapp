# 🛠 Команды для работы с Базой Данных и Сервером

Этот файл содержит полезные команды для управления базой данных, сброса состояния и отладки.

## 🗑️ Полный сброс базы данных (Hard Reset)

Эти команды полностью удаляют базу данных и все данные, создавая чистое состояние (как при первом запуске).

```bash
# 1. Остановить контейнеры и удалить volume с данными
docker compose down -v

# 2. (Опционально) Удалить неиспользуемые volumes, чтобы точно очистить все
docker volume prune -f

# 3. Запустить проект заново с пересборкой
docker compose up --build -d
```

После этого база данных будет пустой (0 пользователей). Первый вход в приложение создаст нового пользователя с ID=1.

## 🔍 Проверка данных в БД (без входа в контейнер)

Используйте эти команды, чтобы быстро проверить, что записано в файле базы данных на диске.

### Проверить количество пользователей
```bash
docker exec telegram-mini-app-backend python -c "
import sqlite3
conn = sqlite3.connect('/app/data/database.db')
cursor = conn.cursor()
cursor.execute('SELECT COUNT(*) FROM users')
print(f'👥 Пользователей в БД: {cursor.fetchone()[0]}')
conn.close()
"
```

### Показать всех пользователей (ID, Username, Имя)
```bash
docker exec telegram-mini-app-backend python -c "
import sqlite3
conn = sqlite3.connect('/app/data/database.db')
cursor = conn.cursor()
cursor.execute('SELECT id, telegram_id, username, first_name FROM users')
print('📋 Список пользователей:')
for row in cursor.fetchall():
    print(f'ID: {row[0]} | TG: {row[1]} | @{row[2]} | {row[3]}')
conn.close()
"
```

### Проверить количество услуг
```bash
docker exec telegram-mini-app-backend python -c "
import sqlite3
conn = sqlite3.connect('/app/data/database.db')
cursor = conn.cursor()
cursor.execute('SELECT COUNT(*) FROM services')
print(f'💆‍♀️ Услуг в БД: {cursor.fetchone()[0]}')
conn.close()
"
```

## 📜 Логи и Отладка

### Посмотреть логи бэкенда (последние 100 строк)
```bash
docker logs telegram-mini-app-backend --tail 100 -f
```
*Флаг `-f` позволяет следить за логами в реальном времени (Ctrl+C для выхода).*

### Проверить путь к базе данных в логах
```bash
docker logs telegram-mini-app-backend --tail 50 | grep "Настройки БД"
```
*Должно быть: `URL=sqlite+aiosqlite:////app/data/database.db`*

## 💾 Бэкап и Восстановление

### Скачать базу данных на локальную машину (из контейнера)
```bash
# 1. Сначала достаем файл из контейнера на сервер
docker cp telegram-mini-app-backend:/app/data/database.db ./database.db
```

### 📥 Скачать с сервера на свой компьютер (в папку Загрузки)

Выполните эти команды **на своем компьютере** (откройте новый терминал, не внутри SSH):

#### Способ 1: Пошагово
```bash
# Шаг 1: Скопировать БД из Docker контейнера во временную папку на сервере
ssh root@178.72.136.133 "docker cp telegram-mini-app-backend:/app/data/database.db /tmp/database_copy.db"

# Шаг 2: Скачать файл в папку Загрузки
scp root@178.72.136.133:/tmp/database_copy.db ~/Downloads/database.db

# Шаг 3 (опционально): Удалить временный файл с сервера
ssh root@178.72.136.133 "rm /tmp/database_copy.db"
```

#### Способ 2: Одной командой (с меткой времени)
```bash
ssh root@178.72.136.133 "docker cp telegram-mini-app-backend:/app/data/database.db /tmp/database_copy.db" && scp root@178.72.136.133:/tmp/database_copy.db ~/Downloads/database_$(date +%Y%m%d_%H%M%S).db && ssh root@178.72.136.133 "rm /tmp/database_copy.db"
```

*Эта команда создаст файл с именем типа `database_20251122_001500.db` в папке Загрузки.*

### Зайти внутрь контейнера бэкенда
```bash
docker exec -it telegram-mini-app-backend /bin/bash
```
*Внутри контейнера база лежит по пути `/app/data/database.db`*

## 🔄 Быстрый перезапуск (без удаления данных)

Если вы изменили код (Python), но хотите сохранить данные в БД:

```bash
docker compose restart backend
```
*Или для полного обновления контейнеров:*
```bash
docker compose up -d --build backend
```
