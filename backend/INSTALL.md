# 🚀 Быстрый старт

## 📦 Установка

### 1. Создание виртуального окружения

```bash
cd backend
python3 -m venv venv
```

### 2. Установка зависимостей

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Настройка .env файла

Убедитесь, что файл `backend/.env` содержит:

```env
BOT_TOKEN=ваш_токен_от_BotFather
WEB_APP_URL=https://zhoasss.github.io/frontend/index.html
```

## ▶️ Запуск

### Через скрипты (рекомендуется):

```bash
# Терминал 1 - API сервер
./start_api.sh

# Терминал 2 - Telegram бот  
./start_bot.sh
```

### Вручную:

```bash
# Терминал 1 - API сервер
cd backend
source venv/bin/activate
python api_server.py

# Терминал 2 - Telegram бот
cd backend
source venv/bin/activate
python main.py
```

## ✅ Проверка работы

### API сервер должен показать:
```
🔍 Путь к .env файлу: /Users/.../backend/.env
🔍 Файл существует: True
🚀 Запуск API сервера...
✅ База данных инициализирована
📁 Путь к БД: /Users/.../backend/database.db
INFO:     Started server process
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Telegram бот должен показать:
```
🔍 Путь к .env файлу: /Users/.../backend/.env
🔍 Файл существует: True
🚀 Запуск Telegram бота...
✅ База данных инициализирована
📁 Путь к БД: /Users/.../backend/database.db
✅ Бот успешно запущен
```

## 🗄️ База данных

После первого запуска создается файл:
```
backend/database.db
```

**Формат:** SQLite 3.x  
**Размер:** ~16 KB (пустая БД)  
**Таблицы:** `users`

## 🧪 Тестирование

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите "Открыть кабинет 🚀"
4. Откройте профиль
5. Отредактируйте данные и сохраните
6. Данные сохранятся в `database.db`!

## 📚 Полная документация

См. `README.md` для подробной информации.

