# Backend - Telegram Bot + FastAPI

## 🏗️ Архитектура

Проект состоит из двух частей:
1. **Telegram бот** (`main.py`) - обрабатывает команды и открывает WebApp
2. **API сервер** (`api_server.py`) - обрабатывает запросы от WebApp и работает с БД

## 📦 Установка зависимостей

```bash
pip install -r requirements.txt
```

## ⚙️ Конфигурация

Создайте файл `.env` в директории `backend/`:

```env
BOT_TOKEN=your_telegram_bot_token
WEB_APP_URL=https://zhoasss.github.io/frontend/index.html
```

## 🚀 Запуск

### Вариант 1: Через скрипты (рекомендуется)

```bash
# Терминал 1 - API сервер
./start_api.sh

# Терминал 2 - Telegram бот
./start_bot.sh
```

### Вариант 2: Вручную

```bash
# Терминал 1 - API сервер
python3 api_server.py

# Терминал 2 - Telegram бот
python3 main.py
```

## 🗄️ База данных

### SQLite
- Файл БД: `backend/database.db`
- Автоматически создается при первом запуске
- Таблицы создаются автоматически

### Модели:

**User** - профили пользователей:
- `telegram_id` (PK) - ID пользователя Telegram
- `first_name` - Имя
- `last_name` - Фамилия  
- `username` - Username
- `phone` - Телефон
- `business_name` - Название бизнеса
- `address` - Адрес
- `created_at` - Дата создания
- `updated_at` - Дата обновления

## 🔌 API Endpoints

### GET `/api/profile/`
Получить профиль пользователя

**Headers:**
- `X-Init-Data`: initData от Telegram WebApp

**Response:**
```json
{
  "telegram_id": 123456789,
  "first_name": "Иван",
  "last_name": "Иванов",
  "username": "ivan",
  "phone": "+7 999 123-45-67",
  "business_name": "Моя парикмахерская",
  "address": "ул. Пушкина, д. 10",
  "created_at": "2025-11-14T...",
  "updated_at": "2025-11-14T..."
}
```

### PUT `/api/profile/`
Обновить профиль пользователя

**Headers:**
- `X-Init-Data`: initData от Telegram WebApp

**Body:**
```json
{
  "phone": "+7 999 123-45-67",
  "business_name": "Моя парикмахерская",
  "address": "ул. Пушкина, д. 10"
}
```

**Response:**
```json
{
  "telegram_id": 123456789,
  "phone": "+7 999 123-45-67",
  "business_name": "Моя парикмахерская",
  "address": "ул. Пушкина, д. 10",
  ...
}
```

## 🔐 Безопасность

### Валидация Telegram initData

Все запросы к API проверяются на подлинность:
1. Извлекается `X-Init-Data` из заголовков
2. Проверяется HMAC-SHA256 подпись
3. Извлекаются данные пользователя
4. Запрос выполняется только для аутентифицированного пользователя

## 🧪 Тестирование API

### Через curl:

```bash
# Получить профиль
curl -X GET http://localhost:8000/api/profile/ \
  -H "X-Init-Data: telegram_init_data_here"

# Обновить профиль
curl -X PUT http://localhost:8000/api/profile/ \
  -H "X-Init-Data: telegram_init_data_here" \
  -H "Content-Type: application/json" \
  -d '{"phone": "+7 999 123-45-67"}'
```

### Через браузер:

Откройте http://localhost:8000/docs для Swagger UI

## 📁 Структура

```
backend/
├── main.py                     # Telegram бот
├── api_server.py               # FastAPI сервер
├── requirements.txt            # Зависимости
├── .env                        # Конфигурация (создайте сами)
├── database.db                 # SQLite БД (создается автоматически)
├── start_bot.sh               # Запуск бота
├── start_api.sh               # Запуск API
└── src/
    ├── shared/                 # Общие компоненты
    │   ├── auth/              # Аутентификация
    │   │   └── telegram_auth.py
    │   ├── config/            # Конфигурация
    │   │   └── env_loader.py
    │   ├── database/          # База данных
    │   │   ├── models.py
    │   │   └── connection.py
    │   └── logger/            # Логирование
    │       └── setup.py
    └── features/              # Функциональность
        ├── api/               # API endpoints
        │   └── profile.py
        └── start_command/     # Команда /start
            ├── handler.py
            └── message_template.py
```

## 🔧 Troubleshooting

### Ошибка: "No module named 'fastapi'"
```bash
pip install -r requirements.txt
```

### Ошибка: "Unable to create event loop"
Используйте Python 3.12 или новее

### API сервер не отвечает
- Проверьте, что сервер запущен
- Проверьте порт 8000
- Проверьте логи сервера

### Данные не сохраняются в БД
- Проверьте, создан ли файл `database.db`
- Проверьте логи API сервера
- Убедитесь, что `X-Init-Data` передается в заголовках

## 📚 Зависимости

- `python-telegram-bot==21.0.1` - Telegram Bot API
- `python-dotenv==1.0.0` - Переменные окружения
- `fastapi==0.104.1` - Web framework
- `uvicorn[standard]==0.24.0` - ASGI сервер
- `sqlalchemy==2.0.23` - ORM для работы с БД
- `aiosqlite==0.19.0` - Асинхронный драйвер SQLite

## 🌐 Production

Для деплоя на сервер:
1. Используйте PostgreSQL вместо SQLite
2. Настройте HTTPS
3. Используйте gunicorn + uvicorn workers
4. Настройте systemd для автозапуска

