# Page Loader Component

Компонент для отображения анимации загрузки на всех страницах приложения с автоматическим определением темы устройства.

## Структура

```
shared/ui/loader/
├── loader.css    # Стили loader'а с поддержкой светлой/тёмной темы
├── loader.js     # Логика управления loader'ом
├── demo.html     # Демо страница
└── README.md     # Документация
```

## Особенности

✨ **Автоматическое определение темы** - loader адаптируется под светлую или тёмную тему устройства  
⏱️ **Минимальное время показа** - 1.5-2 секунды для плавного UX  
🎨 **Адаптивные цвета** - разные цветовые схемы для светлой и тёмной темы  
🔄 **Плавные переходы** - анимация появления/исчезновения  

## Использование

### Автоматическое использование

Loader автоматически:
- Показывается при загрузке страницы
- Отображается минимум 1.5-2 секунды (случайное время для естественности)
- Адаптируется под тему устройства (светлая/тёмная)
- Скрывается после полной загрузки страницы (событие `window.load`)
- Имеет плавную анимацию появления/исчезновения

### Ручное управление

Если вам нужно управлять loader'ом вручную (например, при AJAX запросах):

```javascript
import pageLoader from '../../shared/ui/loader/loader.js';

// Показать loader
pageLoader.show();

// Скрыть loader
pageLoader.hide();

// Скрыть loader с задержкой (в миллисекундах)
pageLoader.hideAfter(500);
```

### Пример использования с API запросами

```javascript
import pageLoader from '../../shared/ui/loader/loader.js';

async function loadData() {
  // Показываем loader перед запросом
  pageLoader.show();
  
  try {
    const response = await fetch('/api/data');
    const data = await response.json();
    
    // Обрабатываем данные
    displayData(data);
  } catch (error) {
    console.error('Error:', error);
  } finally {
    // Скрываем loader после завершения
    pageLoader.hide();
  }
}
```

## Стили

Loader автоматически адаптируется под тему устройства используя `prefers-color-scheme`.

### Светлая тема (по умолчанию):
- **Фон**: `rgba(255, 255, 255, 0.95)` - полупрозрачный белый
- **Цвета анимации**: `#fff`, `#666`, `#aaa`

### Тёмная тема:
- **Фон**: `rgba(0, 0, 0, 0.95)` - полупрозрачный чёрный
- **Цвета анимации**: `#1a1a1a`, `#999`, `#555`

### Общие параметры:
- **Размер**: `--s: 20px` (можно изменить в `loader.css`)
- **Анимация**: 2 секунды, бесконечная
- **Минимальное время показа**: 1.5-2 секунды (случайное)

### Кастомизация

Чтобы изменить размер loader'а:

```css
.loader {
  --s: 30px; /* Увеличить размер */
}
```

Чтобы изменить цвета для светлой темы:

```css
.loader-container {
  --loader-color-1: #your-color-1;
  --loader-color-2: #your-color-2;
  --loader-color-3: #your-color-3;
}
```

Чтобы изменить цвета для тёмной темы:

```css
@media (prefers-color-scheme: dark) {
  .loader-container {
    --loader-color-1: #your-dark-color-1;
    --loader-color-2: #your-dark-color-2;
    --loader-color-3: #your-dark-color-3;
  }
}
```

## Интеграция на новую страницу

1. Добавьте CSS в `<head>`:
```html
<link rel="stylesheet" href="../../shared/ui/loader/loader.css?v=1.0.0">
```

2. Добавьте JS перед закрывающим `</body>`:
```html
<script type="module" src="../../shared/ui/loader/loader.js?v=1.0.0"></script>
```

## Технические детали

### Визуальные параметры:
- **z-index**: 9999 (отображается поверх всего контента)
- **position**: fixed (покрывает весь экран)
- **transition**: opacity 0.3s ease-out (плавное исчезновение)
- **pointer-events**: none (когда скрыт, не блокирует клики)

### Временные параметры:
- **Минимальное время показа**: 1500ms (1.5 секунды)
- **Максимальное время показа**: 2000ms (2 секунды)
- **Случайная задержка**: 1500-2000ms для естественного UX

### Определение темы:
- **Автоматическое**: использует `prefers-color-scheme` media query
- **Динамическое**: отслеживает изменения темы в реальном времени
- **Логирование**: выводит текущую тему в консоль для отладки

## Браузерная совместимость

Loader использует современные CSS и JavaScript функции:
- CSS Grid
- CSS Custom Properties (переменные)
- CSS Animations
- clip-path
- conic-gradient
- prefers-color-scheme media query
- matchMedia API

Поддерживается всеми современными браузерами (Chrome, Firefox, Safari, Edge).
