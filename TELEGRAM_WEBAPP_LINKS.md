# 🔗 Telegram Web App ссылки для бронирования

## Обновление: Ссылки теперь открываются в Telegram

**Дата:** 2025-12-01  
**Версия:** 1.1.0

---

## 🎯 Что изменилось

Теперь при генерации ссылки для бронирования создается **Telegram deep link**, который открывает страницу бронирования **внутри Telegram**, а не в браузере.

### До:
```
https://booking-cab.ru/booking/abc123xy
```
Открывалось в браузере ❌

### После:
```
https://t.me/your_bot?start=booking_abc123xy
```
Открывается в Telegram Web App ✅

---

## 🔧 Технические изменения

### 1. Backend

#### Конфигурация (`backend/src/shared/config/env_loader.py`)
Добавлена новая переменная окружения:
```python
self.bot_username: str = self._get_env("BOT_USERNAME", "your_bot")
```

#### API генерации ссылки (`backend/src/features/api/profiles.py`)
Теперь возвращает Telegram ссылку:
```python
telegram_url = f"https://t.me/{config.bot_username}?start=booking_{new_slug}"

return {
    "booking_url": telegram_url,  # Telegram ссылка (основная)
    "web_url": web_url,  # Обычная веб-ссылка (запасная)
}
```

#### Обработчик бота (`backend/src/features/start_command/handler.py`)
Добавлена поддержка deep links:
```python
if deep_link_param and deep_link_param.startswith('booking_'):
    booking_slug = deep_link_param.replace('booking_', '')
    booking_url = f"{web_app_url}/src/pages/booking/index.html?slug={booking_slug}"
    
    # Показываем кнопку для открытия Web App
    keyboard = [[InlineKeyboardButton(
        "📅 Записаться онлайн",
        web_app=WebAppInfo(url=booking_url)
    )]]
```

### 2. Frontend

#### Настройки (`frontend/src/pages/settings/settings.js`)
Использует Telegram ссылку из API:
```javascript
const bookingUrl = profile.booking_url || `https://t.me/your_bot?start=booking_${profile.booking_slug}`;
```

#### Страница бронирования (`frontend/src/pages/booking/booking.js`)
Поддерживает получение slug из параметра URL:
```javascript
const urlParams = new URLSearchParams(window.location.search);
const slugFromParam = urlParams.get('slug');
const bookingSlug = slugFromParam || slugFromPath;
```

---

## 📋 Настройка

### 1. Добавить переменную окружения

В файл `.env` добавьте:
```bash
BOT_USERNAME=your_bot_username
```

**Где взять username бота:**
1. Откройте @BotFather в Telegram
2. Найдите вашего бота
3. Username указан без @
4. Например, если бот `@MyBookingBot`, укажите `MyBookingBot`

### 2. Обновить конфигурацию на сервере

```bash
# На production сервере
echo "BOT_USERNAME=your_actual_bot_username" >> .env
```

---

## 🚀 Как это работает

### Сценарий использования:

1. **Мастер создает ссылку** в настройках
   - Нажимает "✨ Создать ссылку"
   - Получает: `https://t.me/MyBookingBot?start=booking_abc123xy`

2. **Мастер отправляет ссылку клиенту**
   - В Telegram сообщении
   - В соцсетях
   - В WhatsApp

3. **Клиент нажимает на ссылку**
   - Открывается Telegram
   - Бот отправляет сообщение с кнопкой

4. **Клиент нажимает "📅 Записаться онлайн"**
   - Открывается Web App внутри Telegram
   - Загружается страница бронирования
   - Slug передается через параметр `?slug=abc123xy`

5. **Клиент выбирает услугу и записывается**
   - Все происходит внутри Telegram
   - Нативный опыт использования

---

## 🎨 Преимущества

### Для клиента:
- ✅ Не нужно выходить из Telegram
- ✅ Привычный интерфейс
- ✅ Быстрая загрузка
- ✅ Автоматическая авторизация (если нужно)

### Для мастера:
- ✅ Больше конверсий
- ✅ Меньше отказов
- ✅ Профессиональный вид
- ✅ Легко делиться ссылкой

---

## 🔄 Обратная совместимость

Старые веб-ссылки продолжают работать:
- `https://booking-cab.ru/booking/abc123xy` ✅
- Можно открыть в любом браузере
- Полный функционал сохранен

Новые Telegram ссылки:
- `https://t.me/bot?start=booking_abc123xy` ✅
- Открываются в Telegram
- Рекомендуемый способ

---

## 📱 Примеры

### Генерация ссылки (API Response):
```json
{
  "message": "Ссылка для бронирования создана",
  "booking_slug": "abc123xy",
  "booking_url": "https://t.me/MyBookingBot?start=booking_abc123xy",
  "web_url": "https://booking-cab.ru/booking/abc123xy"
}
```

### Deep Link обработка (Bot):
```
Пользователь: /start booking_abc123xy
Бот: 👋 Привет! Нажмите кнопку ниже, чтобы записаться.
     [📅 Записаться онлайн] <- Web App кнопка
```

### URL параметры (Frontend):
```
https://booking-cab.ru/src/pages/booking/index.html?slug=abc123xy
                                                      ↑
                                            Slug из deep link
```

---

## 🧪 Тестирование

### 1. Локальное тестирование

```bash
# Установить BOT_USERNAME
export BOT_USERNAME=your_test_bot

# Запустить backend
cd backend
python api_server.py

# Сгенерировать ссылку через API
curl -X POST http://localhost:8000/api/profiles/generate-booking-link \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 2. Тестирование в Telegram

1. Создайте тестового бота через @BotFather
2. Укажите его username в `.env`
3. Сгенерируйте ссылку
4. Отправьте себе в Telegram
5. Нажмите на ссылку
6. Проверьте, что открывается Web App

---

## 🐛 Troubleshooting

### Проблема: Ссылка не открывается в Telegram

**Решение:**
1. Проверьте, что `BOT_USERNAME` указан правильно (без @)
2. Проверьте, что бот запущен
3. Проверьте, что Web App URL настроен в @BotFather

### Проблема: Slug не передается

**Решение:**
1. Проверьте логи бота: `docker-compose logs bot`
2. Убедитесь, что deep link параметр передается
3. Проверьте, что frontend читает параметр `?slug=`

### Проблема: Старые ссылки не работают

**Решение:**
- Старые веб-ссылки должны работать
- Проверьте nginx конфигурацию
- Убедитесь, что роутинг настроен правильно

---

## 📝 Миграция

### Для существующих пользователей:

1. **Обновить код:**
   ```bash
   git pull origin dev
   ```

2. **Добавить BOT_USERNAME:**
   ```bash
   echo "BOT_USERNAME=your_bot" >> .env
   ```

3. **Перезапустить сервисы:**
   ```bash
   docker-compose restart
   ```

4. **Перегенерировать ссылки:**
   - Старые ссылки продолжат работать
   - Новые будут создаваться с Telegram форматом

---

## 🎓 Дополнительная информация

### Telegram Deep Links:
- [Документация Telegram](https://core.telegram.org/bots/features#deep-linking)
- [Web App документация](https://core.telegram.org/bots/webapps)

### Формат ссылок:
```
https://t.me/<bot_username>?start=<параметр>
```

Где:
- `<bot_username>` - username бота без @
- `<параметр>` - любая строка (до 64 символов)

---

**Дата создания:** 2025-12-01  
**Версия:** 1.1.0  
**Статус:** ✅ Готово к использованию
