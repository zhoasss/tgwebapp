# 🚀 Деплой функционала "График работы"

## Что было добавлено

### Frontend файлы:
✅ `/frontend/src/pages/schedule/index.html` - обновлен
✅ `/frontend/src/pages/schedule/schedule.js` - создан
✅ `/frontend/src/pages/schedule/schedule.css` - создан
✅ `/frontend/src/shared/lib/schedule-api.js` - создан

### Backend:
✅ API уже существует в `/backend/src/features/api/schedule.py`
✅ Модель WorkingHours уже существует в `/backend/src/shared/database/models.py`

## Шаги для деплоя

### 1. Проверка локально (опционально)

Если хотите протестировать локально перед деплоем:

```bash
# Перейти в директорию проекта
cd /Users/romannavickij/Project/zhoasss.github.io

# Запустить Docker контейнеры
make dev

# Или
docker-compose up --build
```

Откройте в браузере: `http://localhost/src/pages/schedule/index.html`

### 2. Коммит изменений

```bash
# Добавить новые файлы
git add frontend/src/pages/schedule/schedule.js
git add frontend/src/pages/schedule/schedule.css
git add frontend/src/shared/lib/schedule-api.js
git add frontend/src/pages/schedule/index.html
git add SCHEDULE_FEATURE.md
git add DEPLOY_SCHEDULE.md

# Коммит
git commit -m "feat: Добавлен функционал управления графиком работы

- Создана страница настройки графика работы
- Добавлена возможность установки рабочих часов для каждого дня
- Реализовано управление перерывами
- Добавлена валидация времени
- Создан API клиент для работы с графиком
- Добавлена документация"

# Пуш на сервер
git push origin main
```

### 3. Деплой на сервер

Подключитесь к серверу (вы уже подключены через SSH):

```bash
# Перейти в директорию проекта на сервере
cd /root/zhoasss.github.io  # или путь к вашему проекту

# Получить последние изменения
git pull origin main

# Пересобрать и перезапустить контейнеры
docker-compose down
docker-compose up --build -d

# Проверить статус
docker-compose ps

# Проверить логи (если нужно)
docker-compose logs -f frontend
docker-compose logs -f backend
```

### 4. Проверка работоспособности

1. Откройте Telegram бот
2. Запустите Mini App
3. Перейдите в Настройки → График работы
4. Проверьте, что:
   - ✅ Страница загружается
   - ✅ Отображаются все 7 дней недели
   - ✅ Можно переключать рабочие/выходные дни
   - ✅ Можно изменять время работы
   - ✅ Можно добавлять перерывы
   - ✅ Кнопка "Сохранить" работает
   - ✅ После сохранения и перезагрузки данные сохраняются

### 5. Проверка API

Проверьте, что API работает:

```bash
# На сервере
curl -X GET "http://localhost:8000/api/schedule" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Должен вернуть график работы
```

### 6. Очистка кэша (если нужно)

Если изменения не отображаются:

```bash
# Очистить кэш Docker
docker system prune -f

# Пересобрать с нуля
docker-compose build --no-cache
docker-compose up -d
```

## Быстрый деплой (одной командой)

Если вы уже на сервере и хотите быстро задеплоить:

```bash
cd /root/zhoasss.github.io && \
git pull origin main && \
docker-compose up --build -d && \
docker-compose ps
```

## Откат изменений (если что-то пошло не так)

```bash
# Откатить последний коммит
git revert HEAD

# Или откатить к предыдущей версии
git reset --hard HEAD~1

# Пуш изменений
git push origin main --force

# Перезапустить контейнеры
docker-compose up --build -d
```

## Проверка логов

Если возникли проблемы:

```bash
# Логи frontend
docker-compose logs -f frontend

# Логи backend
docker-compose logs -f backend

# Все логи
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100
```

## Мониторинг

После деплоя проверьте:

```bash
# Статус контейнеров
docker-compose ps

# Использование ресурсов
docker stats

# Health check
curl http://localhost/health
curl http://localhost:8000/health
```

## Troubleshooting

### Проблема: Страница не загружается

**Решение:**
```bash
# Проверить nginx конфигурацию
docker-compose exec frontend nginx -t

# Перезапустить frontend
docker-compose restart frontend
```

### Проблема: API не работает

**Решение:**
```bash
# Проверить логи backend
docker-compose logs backend

# Проверить, что backend запущен
docker-compose ps backend

# Перезапустить backend
docker-compose restart backend
```

### Проблема: Изменения не применяются

**Решение:**
```bash
# Полная пересборка
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## Контрольный список

- [ ] Все файлы добавлены в git
- [ ] Коммит создан с понятным сообщением
- [ ] Изменения запушены на сервер
- [ ] На сервере выполнен git pull
- [ ] Контейнеры пересобраны и перезапущены
- [ ] Проверен статус контейнеров (все running)
- [ ] Страница открывается в Telegram Mini App
- [ ] Функционал работает корректно
- [ ] Данные сохраняются в БД
- [ ] Нет ошибок в логах

## Полезные команды

```bash
# Проверить версию файлов
git log --oneline -5

# Посмотреть изменения
git diff HEAD~1

# Проверить статус git
git status

# Посмотреть активные контейнеры
docker ps

# Зайти внутрь контейнера
docker-compose exec backend bash
docker-compose exec frontend sh

# Проверить БД
docker-compose exec backend python -c "
from src.shared.database.connection import get_session
from src.shared.database.models import WorkingHours
import asyncio

async def check():
    async for session in get_session():
        result = await session.execute('SELECT COUNT(*) FROM working_hours')
        print(f'Working hours records: {result.scalar()}')

asyncio.run(check())
"
```

## Следующие шаги

После успешного деплоя графика работы, можно приступить к:

1. **Интеграция с записями** - использовать график для определения доступных слотов
2. **Публичное бронирование** - показывать график клиентам
3. **Уведомления** - напоминания о рабочих часах
4. **Аналитика** - статистика по загруженности

---

**Дата создания:** 2025-11-22  
**Автор:** AI Assistant  
**Версия:** 1.0.0
