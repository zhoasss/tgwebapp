# Диагностика HTTP 401 ошибок аутентификации

## 📋 Обзор проблемы

Браузер не отправляет cookies с токенами в API запросы, вследствие чего все защищенные endpoint'ы возвращают 401 Unauthorized.

### Обнаруженные симптомы:
```
❌ Failed to load resource: the server responded with a status of 401 (me)
❌ Failed to load resource: the server responded with a status of 401 (appointments)
❌ Failed to load resource: the server responded with a status of 401 (refresh)
```

### На сервере логи показывают:
```
access_token from cookie: None
refresh_token from cookie: None
```

---

## 🔍 Диагностический процесс

### Шаг 1: Подготовка браузера

```bash
# 1. Очистить кэш браузера
- На Windows/Linux: Ctrl+Shift+Delete
- На Mac: Cmd+Shift+Delete

# 2. Hard refresh страницы
- На Windows/Linux: Ctrl+Shift+R
- На Mac: Cmd+Shift+R

# 3. Открыть Developer Tools
- F12 (Windows/Linux) или Cmd+Option+I (Mac)

# 4. Перейти на вкладку Console
```

### Шаг 2: Выполнить инициализацию приложения

1. Загрузить страницу приложения
2. Попытаться залогиниться (если не залогинены)
3. Выполнить действие, требующее аутентификации

### Шаг 3: Анализировать логи консоли

В консоли браузера (F12 → Console) будут видны логи с префиксами:

- 🚀 - Начало процесса
- 🔍 - Диагностическая информация
- 📋 - Детали операции
- 📡 - Сетевые операции
- ✅ - Успешные операции
- ❌ - Ошибки и проблемы
- ⚠️  - Предупреждения

---

## 🧪 Диагностические сценарии

### Сценарий 1: Приложение не запущено в Telegram WebApp

**Признаки в логах:**
```
🚀 === LOGIN ATTEMPT START ===
📋 LOGIN DIAGNOSTICS: {hasInitData: false, ...}
❌ FATAL: initData is null or undefined
   This means app is NOT running inside Telegram WebApp
🔍 Telegram availability: {
  hasTelegramObject: false, 
  hasWebApp: false, 
  hasInitData: false
}
```

**Проблема:** Приложение запущено в обычном браузере или без контекста Telegram.

**Решение:**
- Убедитесь, что приложение открыто через Telegram WebApp
- Правильная ссылка: `https://t.me/BotUsername/appname`
- Приложение должно быть запущено именно из Telegram!

---

### Сценарий 2: Tokens не сохраняются в cookies браузером

**Признаки в логах:**
```
🍪 setCookie: access_token
  - protocol: https:
  - sameSite: SameSite=None; Secure
  - verification: ✅ установлена

🔍 Проверка сохраненных cookies:
   - access_token in cookies: ❌ NO  ← ПРОБЛЕМА!
   - refresh_token in cookies: ❌ NO
   - All cookies: (empty)
```

**Проблема:** Браузер блокирует сохранение cookies.

**Возможные причины:**
1. Проблема с SameSite=None; Secure политикой
2. Браузер блокирует cookies третьих сторон
3. Приватный режим браузера
4. HTTPS сертификат не доверяется

**Решение:**
- Проверить, что используется HTTPS для production
- Убедиться, что `Secure` флаг установлен для HTTPS
- Отключить приватный режим браузера
- Проверить настройки приватности браузера

---

### Сценарий 3: Cookies не отправляются с API запросом

**Признаки в логах:**
```
🍪 access_token in cookies: ✅ YES (после login)

🔍 === CHECK AUTH STATUS ===
✅ Added Authorization header
📡 Response status: 401

❌ === API ERROR ===
DIAGNOSTICS: {
  hasAuthToken: false,
  allCookies: 'EMPTY'  ← Cookies не отправлены!
}
```

**Проблема:** Браузер не отправляет cookies с запросом несмотря на наличие их.

**Возможные причины:**
1. Отсутствует `credentials: 'include'` в fetch конфиге
2. CORS проблема на сервере
3. Domain/protocol несовпадение

**Решение:**
- Проверить, что `credentials: 'include'` есть во всех fetch запросах
- Проверить CORS конфигурацию на сервере
- Убедиться, что frontend и backend на одном протоколе (https/http)

---

### Сценарий 4: Сервер не возвращает токены

**Признаки в логах:**
```
📡 Выполнение входа через initData...
📦 Response data: {
  access_token: 'MISSING',  ← Нет токена!
  refresh_token: 'MISSING'
}
❌ access_token отсутствует в ответе!
```

**Проблема:** Endpoint `/api/auth/signin` не возвращает токены.

**Возможные причины:**
1. Ошибка на сервере в /signin endpoint
2. initData невалидна
3. Ошибка аутентификации на сервере

**Решение:**
- Проверить логи сервера за ошибками
- Убедиться, что initData валидна
- Проверить backend код в `auth.py`

---

### Сценарий 5: Token невалиден или истек

**Признаки в логах:**
```
🔍 === API ERROR ===
Status: 401
DIAGNOSTICS: {
  hasAuthToken: true,
  allCookies: 'access_token=eyJ...'  ← Token есть!
}
```

**Проблема:** Token существует, но сервер его не принимает.

**Возможные причины:**
1. Token истек (refresh нужен)
2. Token невалиден (скомпрометирован)
3. Проблема с JWT validation на сервере
4. Разные secret keys на клиенте и сервере

**Решение:**
- Логаут и новый вход
- Проверить, что refresh token работает
- Проверить JWT secret key на сервере

---

## 💻 Быстрая диагностика через консоль

Скопируйте и запустите эти команды в консоли браузера (F12 → Console):

### Проверка Telegram контекста
```javascript
console.log({
  hasTelegram: !!window.Telegram,
  hasWebApp: !!window.Telegram?.WebApp,
  hasInitData: !!window.Telegram?.WebApp?.initData,
  initDataLength: window.Telegram?.WebApp?.initData?.length || 0,
  protocol: window.location.protocol,
  host: window.location.host
});
```

### Проверка cookies
```javascript
console.log('Cookies:', document.cookie || 'EMPTY');
```

### Проверка локального хранилища
```javascript
console.log('LocalStorage:', {
  accessToken: localStorage.getItem('access_token') ? 'YES' : 'NO',
  refreshToken: localStorage.getItem('refresh_token') ? 'YES' : 'NO'
});
```

### Проверка sessionStorage
```javascript
console.log('SessionStorage:', {
  accessToken: sessionStorage.getItem('access_token') ? 'YES' : 'NO',
  refreshToken: sessionStorage.getItem('refresh_token') ? 'YES' : 'NO'
});
```

### Чтение актуального cookie
```javascript
function getCookie(name) {
  const nameEQ = name + "=";
  const ca = document.cookie.split(';');
  for(let i=0;i < ca.length;i++) {
    let c = ca[i].trim();
    if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length);
  }
  return null;
}

console.log('Access Token:', getCookie('access_token')?.substring(0, 50) + '...' || 'NOT FOUND');
```

---

## 📊 Версия истории

| Версия | Изменения |
|--------|-----------|
| v3.0.4 | Основные исправления: `credentials: 'include'` + SameSite cookies |
| v3.0.5 | Basic логирование: cookies operations, API requests |
| v3.0.6 | Comprehensive логирование: login flow, token verification |
| v3.0.7 | Detailed диагностика: error context, Telegram availability |

---

## 🚀 Переразвертывание

```bash
# 1. Обновить код
git pull origin dev

# 2. Пересобрать контейнеры
docker-compose up -d --build

# 3. Очистить кэш браузера
# Ctrl+Shift+Delete / Cmd+Shift+Delete

# 4. Hard refresh
# Ctrl+Shift+R / Cmd+Shift+R

# 5. Открыть F12 и смотреть логи
```

---

## 📝 Отправка отчета о проблеме

При создании отчета об ошибке, пожалуйста предоставьте:

1. **Скриншот консоли браузера** с логами ошибок
2. **Логи сервера** за время ошибки
3. **Информацию о браузере**:
   ```javascript
   // В консоли:
   navigator.userAgent
   ```
4. **Контекст запуска**:
   - Запущено ли приложение в Telegram?
   - HTTP или HTTPS?
   - Какой домен?
5. **Точные шаги для воспроизведения ошибки**

---

## 🔗 Связанные файлы

- `/frontend/src/shared/lib/jwt-auth.js` - JWT аутентификация
- `/frontend/src/shared/lib/api-client.js` - API клиент
- `/frontend/src/shared/lib/cookies.js` - Управление cookies
- `/frontend/src/shared/lib/telegram.js` - Интеграция Telegram
- `/backend/src/features/api/auth.py` - Backend аутентификация

---

**Последнее обновление:** 5 декабря 2025 г. (v3.0.7)
