# 🤖 Двухботовая система для записи клиентов

**Дата:** 2025-12-03  
**Версия:** 2.0.0

---

## 🎯 Концепция

Теперь система использует **два отдельных бота**:

### 1. **Основной бот** (Admin Bot)
- Для владельца бизнеса
- Управление услугами, расписанием, записями
- Настройки профиля
- Генерация ссылок для клиентов

### 2. **Клиентский бот** (@booking_cab_bot)
- Только для клиентов
- Открывает форму записи
- Чистый интерфейс без лишних функций
- Профессиональный вид

---

## ✨ Преимущества двухботовой системы

### Для владельца бизнеса:
- ✅ Разделение интерфейсов (админ и клиент)
- ✅ Клиенты не видят админские функции
- ✅ Профессиональный имидж
- ✅ Можно настроить разные приветствия

### Для клиентов:
- ✅ Простой и понятный интерфейс
- ✅ Только кнопка "Записаться"
- ✅ Не путаются в функциях
- ✅ Быстрый доступ к записи

---

## 🔧 Технические изменения

### 1. Backend

#### Конфигурация (`env_loader.py`)
```python
# Основной бот (для админа)
self.bot_token: str = self._get_required_env("BOT_TOKEN")
self.bot_username: str = self._get_env("BOT_USERNAME", "your_bot")

# Клиентский бот (опционально)
self.client_bot_token: Optional[str] = self._get_env("CLIENT_BOT_TOKEN")
self.client_bot_username: Optional[str] = self._get_env("CLIENT_BOT_USERNAME")
```

#### Аутентификация (`telegram_auth.py`)
- Поддержка валидации через несколько токенов
- Если `initData` валиден любым токеном — авторизация успешна
- Автоматическое определение, из какого бота пришел пользователь

#### Генерация ссылок (`profiles.py`)
```python
# Используем username клиентского бота, если настроен
bot_username = config.client_bot_username or config.bot_username
telegram_url = f"https://t.me/{bot_username}?start=booking_{new_slug}"
```

### 2. Новые файлы

#### `backend/client_bot.py`
Точка входа для клиентского бота:
- Отдельный процесс
- Собственное логирование (`client_bot.log`)
- Использует `CLIENT_BOT_TOKEN`

#### `backend/src/features/start_command/client_bot_handler.py`
Обработчик команд для клиентского бота:
- Только обработка deep links с `booking_`
- Простое приветствие без deep link
- Логирование с префиксом `[CLIENT BOT]`

#### `backend/start_client_bot.sh`
Скрипт запуска клиентского бота для локальной разработки

### 3. Docker Compose

Добавлен новый сервис `client-bot`:
```yaml
client-bot:
  container_name: telegram-mini-app-client-bot
  command: ["python", "client_bot.py"]
  profiles:
    - client-bot  # Опционально
```

---

## 📋 Настройка

### 1. Переменные окружения

Добавьте в `.env`:
```bash
# Основной бот (Admin)
BOT_TOKEN=your_admin_bot_token
BOT_USERNAME=your_admin_bot_username

# Клиентский бот
CLIENT_BOT_TOKEN=8428950840:AAHh-TvnjYL2seOl_StLRqRsyuFg7eKpp7I
CLIENT_BOT_USERNAME=booking_cab_bot

# Web App URL
WEB_APP_URL=https://booking-cab.ru
```

### 2. Настройка клиентского бота в @BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/mybots` → выберите `@booking_cab_bot`
3. `Bot Settings` → `Menu Button` → `Configure menu button`
4. URL: `https://booking-cab.ru`
5. Text: `📅 Записаться`

### 3. Запуск

#### Локально (для разработки):
```bash
# Терминал 1 - Backend API
cd backend
python api_server.py

# Терминал 2 - Основной бот
cd backend
./start_bot.sh

# Терминал 3 - Клиентский бот
cd backend
./start_client_bot.sh
```

#### Docker (Production):
```bash
# Запуск всех сервисов включая клиентского бота
docker-compose --profile client-bot up -d

# Или без клиентского бота (будет использоваться основной)
docker-compose up -d
```

---

## 🚀 Как это работает

### Сценарий 1: Владелец бизнеса

1. Открывает основного бота
2. Получает приветствие с кнопкой "Открыть приложение"
3. Управляет своим бизнесом
4. Генерирует ссылку для клиентов

### Сценарий 2: Клиент записывается

1. Получает ссылку: `https://t.me/booking_cab_bot?start=booking_abc123`
2. Нажимает на ссылку
3. Открывается `@booking_cab_bot`
4. Видит сообщение: "👋 Привет! Нажмите кнопку ниже..."
5. Нажимает "📅 Записаться онлайн"
6. Открывается Web App с формой записи
7. Выбирает услугу и время
8. Записывается

---

## 🔄 Обратная совместимость

### Если CLIENT_BOT_TOKEN не указан:
- Система работает как раньше
- Используется только основной бот
- Ссылки генерируются на основного бота
- Все функции доступны

### Если CLIENT_BOT_TOKEN указан:
- Ссылки генерируются на клиентского бота
- Основной бот — только для админа
- Клиентский бот — только для записи
- Оба бота работают параллельно

---

## 📊 Логирование

### Основной бот:
```
🤖 Инициализация Telegram бота...
✅ Telegram бот успешно запущен!
```

### Клиентский бот:
```
🤖 [CLIENT BOT] Инициализация клиентского Telegram бота...
✅ [CLIENT BOT] Telegram бот успешно запущен!
📱 [CLIENT BOT] Получена команда /start от пользователя...
```

Логи сохраняются в:
- `backend/data/bot.log` — основной бот
- `backend/data/client_bot.log` — клиентский бот

---

## 🐛 Troubleshooting

### Проблема: Клиентский бот не запускается

**Решение:**
1. Проверьте, что `CLIENT_BOT_TOKEN` указан в `.env`
2. Проверьте логи: `docker-compose logs client-bot`
3. Убедитесь, что токен правильный

### Проблема: Ссылки ведут на старого бота

**Решение:**
1. Перегенерируйте ссылку в настройках
2. Проверьте, что `CLIENT_BOT_USERNAME` указан
3. Перезапустите backend

### Проблема: Авторизация не работает

**Решение:**
1. Оба токена должны быть валидными
2. Проверьте логи backend: `docker-compose logs backend`
3. Убедитесь, что Web App URL настроен в обоих ботах

---

## 🎓 Дополнительная информация

### Архитектура:

```
┌─────────────────┐
│  Основной бот   │ ← Владелец бизнеса
│   (Admin Bot)   │
└─────────────────┘
        ↓
┌─────────────────┐
│    Backend API  │ ← Общая база данных
└─────────────────┘
        ↑
┌─────────────────┐
│ Клиентский бот  │ ← Клиенты
│  (Client Bot)   │
└─────────────────┘
```

### Безопасность:

- ✅ Оба бота используют одну базу данных
- ✅ Авторизация через Telegram initData
- ✅ JWT токены для API
- ✅ Валидация через оба токена

---

## 📝 Миграция с одноботовой системы

### Шаг 1: Создать клиентского бота
1. @BotFather → `/newbot`
2. Получить токен

### Шаг 2: Обновить код
```bash
git pull origin dev
```

### Шаг 3: Добавить переменные
```bash
echo "CLIENT_BOT_TOKEN=ваш_токен" >> .env
echo "CLIENT_BOT_USERNAME=ваш_username" >> .env
```

### Шаг 4: Перезапустить
```bash
docker-compose --profile client-bot up -d
```

### Шаг 5: Перегенерировать ссылки
- Зайти в настройки
- Удалить старую ссылку
- Создать новую (она будет вести на клиентского бота)

---

**Дата создания:** 2025-12-03  
**Версия:** 2.0.0  
**Статус:** ✅ Готово к использованию  
**Клиентский бот:** @booking_cab_bot
